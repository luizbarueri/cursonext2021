import Layout from '../../Components/Layout';

export default function Navegacao(props) {
    return (
        <Layout titulo = {props.titulo}>
            <h1>Navegação #01</h1>
        </Layout>
    )
} 